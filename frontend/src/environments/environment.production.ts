export const environment = {
  production: true,
  /**
   * Absolute URL of the Nest API on Render (no trailing slash, no `/api` prefix).
   * Cloudflare Pages build can override via `API_BASE_URL` + `yarn build:pages`.
   */
  apiBaseUrl: 'https://YOUR-API.onrender.com',
};
